<p>
	<strong>the title is:</strong>
	<%= @article.title %>
</p>

<p>
	<strong>the text is:</strong>
	<%= @article.text %>
</p>

<h2>COMMENTS:</h2>
<%= render @article.comments %>

<h2>add yo comment?</h2>
<%= render "comments/form" %>

<%= link_to 'Back', articles_path %> | <%= link_to 'Edit', edit_article_path(@article) %>